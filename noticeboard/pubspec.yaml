name: noticeboard
description: "A serverless talking noticeboard."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.2+1

environment:
  sdk: ^3.6.0-216.1.beta

dependencies:
  auto_size_text: ^3.0.0
  backstreets_widgets: ^0.23.1
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_audio_games: ^0.31.0
  flutter_riverpod: ^2.5.3
  flutter_soloud: ^2.1.5
  flutter_tts: ^4.0.2
  json_annotation: ^4.9.0
  path: ^1.9.0
  riverpod_annotation: ^2.5.3
  shared_preferences: ^2.3.2
  window_manager: ^0.4.3

dev_dependencies:
  build_runner: ^2.4.13
  custom_lint: ^0.7.0
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.8.0
  riverpod_generator: ^2.4.3
  riverpod_lint: ^2.3.13

flutter:
  uses-material-design: true
  assets:
    - sounds/
