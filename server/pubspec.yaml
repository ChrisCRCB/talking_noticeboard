name: server
description: A sample command-line application.
version: 1.0.0
publish_to: none
environment:
  sdk: ^3.1.0-262.2.beta

# Add regular dependencies here.
dependencies:
  noticeboard_backend:
    path: ../noticeboard_backend
  path: ^1.8.3
  shelf: ^1.4.1
  shelf_cors_headers: ^0.1.5
  shelf_plus: ^1.7.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
